import { PrayerWall } from '@/components/community/PrayerWall';

// Tab route — no back button, extra bottom padding for the floating tab bar.
export default function CommunityScreen() {
  return <PrayerWall />;
}
