# DailyPrayer — Environment Variables Example
#
# Copy to .env and fill in. .env is gitignored — never commit it.
#
# NOTE: Anything prefixed EXPO_PUBLIC_ is inlined into the JS bundle at build
# time and is readable by anyone who downloads the app. Only put values here
# that are safe to publish. Secret keys belong on a server.

# ── Supabase ──────────────────────────────────────────────
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# ── RevenueCat (public SDK keys — safe to ship) ───────────
EXPO_PUBLIC_REVENUECAT_API_KEY_IOS=appl_your_ios_key
EXPO_PUBLIC_REVENUECAT_API_KEY_ANDROID=goog_your_android_key
# Test Store key for local development ("test_…"). The app refuses to use
# this in a release build — production needs the two platform keys above.
EXPO_PUBLIC_REVENUECAT_API_KEY_TEST=test_your_test_store_key

# ── Bible API ─────────────────────────────────────────────
# Optional. Overrides the default translation id used against the
# wldeh/bible-api CDN. Must be a public-domain edition.
# EXPO_PUBLIC_BIBLE_API_VERSION=en-kjv

# ── AI assistant (optional) ───────────────────────────────
# The AI companion talks ONLY to a server proxy that holds the provider key.
# Never put a provider key in this file: every EXPO_PUBLIC_ value is inlined
# into the JS bundle and can be read out of any installed copy of the app.
#
# The proxy accepts an OpenAI-shaped POST { model, messages } and attaches the
# real credential itself. It is also the only place a per-user rate limit can
# be enforced — the in-app limiter is just a runaway-loop guard.
#
# Leave this unset and the AI tab honestly reports itself unavailable and
# shows offline reflections instead.
# EXPO_PUBLIC_AI_PROXY_URL=https://your-proxy.example.com/v1/chat/completions
